name: Fix and verify compaction summary

on:
  push:
    branches: [codex-compat]

permissions:
  contents: write

jobs:
  fix:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex-compat
          fetch-depth: 0

      - name: Apply focused fixes
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path

          def replace_once(path, old, new):
              p = Path(path)
              text = p.read_text()
              n = text.count(old)
              if n != 1:
                  raise SystemExit(f"{path}: expected one anchor, found {n}")
              p.write_text(text.replace(old, new, 1))

          path = "models/templates/llama-cpp-qwen3.8-codex.jinja"
          replace_once(
              path,
              """        'You are performing a CONTEXT CHECKPOINT COMPACTION. '
        ~ 'Do not call or use any tools. Create a handoff summary for another language model that will resume the task. '
        ~ 'Include current progress and key decisions, important context and constraints, remaining work, and critical data, examples, or references needed to continue. '
        ~ 'Be concise, structured, and output only the handoff summary.'
""",
              """        'Create a persistent CONTEXT CHECKPOINT for another language model that will resume this exact task. '
        ~ 'Do not call or use tools and do not emit an empty response. Write the checkpoint directly as final-answer content, without analysis or preamble. '
        ~ 'Preserve current progress and key decisions, exact technical facts and measurements, important context and constraints, remaining work and next steps, and critical commands, paths, examples, or references needed to continue. '
        ~ 'Be concise but lossless. Your response must continue after the heading "## Context checkpoint" with a non-empty Markdown handoff summary.'
""",
          )
          replace_once(
              path,
              """{%- if add_generation_prompt %}
    {{- '<|im_start|>assistant\\n' }}
    {%- if not _thinking_enabled %}
        {{- '<think>\\n\\n</think>\\n\\n' }}
    {%- else %}
        {{- '<think>\\n' }}
    {%- endif %}
{%- endif %}""",
              """{%- if add_generation_prompt %}
    {{- '<|im_start|>assistant\\n' }}
    {%- if is_compaction | default(false) %}
        {{- '<think>\\n\\n</think>\\n\\n## Context checkpoint\\n' }}
    {%- elif not _thinking_enabled %}
        {{- '<think>\\n\\n</think>\\n\\n' }}
    {%- else %}
        {{- '<think>\\n' }}
    {%- endif %}
{%- endif %}""",
          )

          path = "tools/server/server-task.cpp"
          replace_once(
              path,
              """static json server_task_build_response_compaction(const std::string & summary) {
    return json {
""",
              """static json server_task_build_response_compaction(const std::string & summary) {
    if (summary.find_first_not_of(" \\t\\r\\n") == std::string::npos) {
        throw std::runtime_error("Responses compaction generated an empty summary");
    }

    return json {
""",
          )
          PY

          git diff --check

      - name: Configure CPU-only server build
        run: cmake -S . -B build-ci -DGGML_NATIVE=OFF -DGGML_VULKAN=OFF -DGGML_CUDA=OFF -DLLAMA_CURL=OFF -DCMAKE_BUILD_TYPE=Release

      - name: Build llama-server
        run: cmake --build build-ci --target llama-server -j2

      - name: Commit verified fix
        shell: bash
        run: |
          set -euo pipefail
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add models/templates/llama-cpp-qwen3.8-codex.jinja tools/server/server-task.cpp
          git commit -m 'server: make Responses compaction summaries robust'
          git push origin HEAD:codex-compat
